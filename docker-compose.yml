services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///app/app/data/app.db
    volumes:
      - app-data:/app/app/data
volumes:
  app-data:
